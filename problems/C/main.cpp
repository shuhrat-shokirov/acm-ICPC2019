#include <bits/stdc++.h>

using namespace std;

long long b[100000];

/*int main() {
    //freopen("input.txt", "r", stdin);
    //freopen("output.txt", "w", stdout);
    int n;
    cin >> n;
    long long a[n + 1], k = 0;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
        if (a[i] <= n) {
            if (b[a[i]] == 0) {
                k++;
                b[a[i]]++;
            }
        }
    }
    cout << k;
}*/