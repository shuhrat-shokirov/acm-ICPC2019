#include <bits/stdc++.h>

using namespace std;
/*
int main() {
    int n;
    cin >> n;
    stack<string> a;
    string b, c;
    for (int i = 0; i < n; i++) {
        cin >> b >> c;
        if (b == "Add") a.push(c);
        else if (!a.empty()) {
            if (a.top() == c) a.pop();
            else continue;
        }else{
            cout << "No";
            return 0;
        }
    }
    if (a.empty())
        cout << "Yes";
    else
        cout << "No";
}
*/